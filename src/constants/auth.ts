// The key used for the user authentication token in cookies.
export const USER_COOKIE_KEY = '__token' as const;

// List of strings representing the routes that require authentication.
export const AUTH_GUARD = [] as const;

// List of strings representing the routes that require only non-authentication
export const USER_GUARD = [] as const;
